# stars
three.js web experience

## GitHub Pages
1. Push this repository to GitHub.
2. In the repository settings, enable **Pages** and set the source to the `main` branch (root).
3. Visit the published URL once GitHub Pages finishes building.

This repo includes a `.nojekyll` file so GitHub Pages serves the static files as-is.
